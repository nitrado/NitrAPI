## GET /ping

The ping resource can be used to check the api health.

Request:
```
GET /ping
```

Example Response:
```json
{
    "status": "success",
    "message": "All systems operate as expected."
}
```